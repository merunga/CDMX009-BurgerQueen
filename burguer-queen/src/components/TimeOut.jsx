import React from 'react'

function TimeOut(props) {
    return (
        <div>
            <button className="btn-danger">✔{props.newDate}</button>
           
        </div>
    )
}

export default TimeOut
